name: Rust CI

on:
  push:
    branches: [ main, develop ]
    paths:
      - 'sketches/**'
  pull_request:
    branches: [ main, develop ]
    paths:
      - 'sketches/**'

env:
  CARGO_TERM_COLOR: always
  RUST_BACKTRACE: 1

jobs:
  test:
    name: Test Suite
    runs-on: ubuntu-latest
    strategy:
      matrix:
        rust: [ stable, nightly ]
    steps:
      - name: Checkout code
        uses: actions/checkout@v4

      - name: Install Rust toolchain
        uses: dtolnay/rust-toolchain@master
        with:
          toolchain: ${{ matrix.rust }}

      - name: Cache dependencies
        uses: actions/cache@v4
        with:
          path: |
            ~/.cargo/registry
            ~/.cargo/git
            sketches/target
          key: ${{ runner.os }}-cargo-${{ matrix.rust }}-${{ hashFiles('sketches/**/Cargo.lock') }}

      - name: Run tests
        working-directory: sketches
        run: cargo test --all-features --verbose

      - name: Run doc tests
        working-directory: sketches
        run: cargo test --doc --all-features

  clippy:
    name: Clippy
    runs-on: ubuntu-latest
    steps:
      - name: Checkout code
        uses: actions/checkout@v4

      - name: Install Rust toolchain
        uses: dtolnay/rust-toolchain@stable
        with:
          components: clippy

      - name: Cache dependencies
        uses: actions/cache@v4
        with:
          path: |
            ~/.cargo/registry
            ~/.cargo/git
            sketches/target
          key: ${{ runner.os }}-cargo-clippy-${{ hashFiles('sketches/**/Cargo.lock') }}

      - name: Run clippy
        working-directory: sketches
        run: cargo clippy --all-targets --all-features -- -D warnings

  fmt:
    name: Rustfmt
    runs-on: ubuntu-latest
    steps:
      - name: Checkout code
        uses: actions/checkout@v4

      - name: Install Rust toolchain
        uses: dtolnay/rust-toolchain@stable
        with:
          components: rustfmt

      - name: Check formatting
        working-directory: sketches
        run: cargo fmt --all -- --check

  build:
    name: Build
    runs-on: ubuntu-latest
    steps:
      - name: Checkout code
        uses: actions/checkout@v4

      - name: Install Rust toolchain
        uses: dtolnay/rust-toolchain@stable

      - name: Cache dependencies
        uses: actions/cache@v4
        with:
          path: |
            ~/.cargo/registry
            ~/.cargo/git
            sketches/target
          key: ${{ runner.os }}-cargo-build-${{ hashFiles('sketches/**/Cargo.lock') }}

      - name: Build release
        working-directory: sketches
        run: cargo build --release --all-features --verbose

      - name: Build examples
        working-directory: sketches
        run: cargo build --examples --all-features
